import{useState}from"react";import{supabase}from"../lib/supabase";export default function Login(){const[error,setError]=useState("");async function login(){const{error}=await supabase.auth.signInWithOAuth({provider:"google",options:{redirectTo:window.location.origin}});if(error)setError(error.message)}return <main className="login"><div className="card loginbox"><h1>Handicap BBC</h1><p>Resultados e administração do torneio</p><button onClick={login}>Entrar com Google</button>{error&&<p className="error">{error}</p>}</div></main>}
